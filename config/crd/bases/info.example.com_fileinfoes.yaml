---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: fileinfoes.info.example.com
spec:
  group: info.example.com
  names:
    kind: FileInfo
    listKind: FileInfoList
    plural: fileinfoes
    singular: fileinfo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FileInfo is the Schema for the fileinfoes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FileInfoSpec defines the desired state of FileInfo.
            properties:
              filePath:
                type: string
            required:
            - filePath
            type: object
          status:
            description: FileInfoStatus defines the observed state of FileInfo.
            properties:
              files:
                items:
                  properties:
                    name:
                      type: string
                    size:
                      format: int64
                      type: integer
                  required:
                  - name
                  - size
                  type: object
                type: array
              nodeName:
                type: string
            required:
            - files
            - nodeName
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
